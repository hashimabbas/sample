/*var filter = document.getElementById('filter')
var doctors = document.getElementById('doctors')
var doctors_name = document.getElementsByTagName('doctor_name');
filter.addEventListener('keyup', filterDoctor);
console.log(doctors_name);
console.log(doctors_name[0].textContent);*/

/*function filterDoctor(e) {
    var text = e.target.value.toLowerCase();
    for (i = 0; i < doctors_name.length; i++) {
        var doctor_name = doctors_name[i].textContent;
        if (doctor_name.toLowerCase().indexOf(text) != -1) {
            doctors_name[i].parentNode.parentNode.parentNode.parentNode.parentNode.classList.remove('d-flex');
            doctors_name[i].parentNode.parentNode.parentNode.parentNode.parentNode.style.display = 'block';
        } else {
            doctors_name[i].parentNode.parentNode.parentNode.parentNode.parentNode.classList.remove('d-flex');
            doctors_name[i].parentNode.parentNode.parentNode.parentNode.parentNode.style.display = 'none';
        }
    }
}*/